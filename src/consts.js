const TRIP_POINTS_COUNT = 4;

const Mode = {
  DEFAULT: 'DEFAULT',
  EDITING: 'EDITING'
};

const FilterType = {
  EVERYTHING: 'everything',
  FUTURE: 'future',
  PRESENT: 'present',
  PAST: 'past',
};

export { TRIP_POINTS_COUNT, Mode, FilterType };
